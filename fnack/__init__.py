"""fnack package root.

Contains the public plugin SDK under `fnack.plugin_api`. The application
itself lives at the repository root (`app.py`); this package exists so
plugins have a stable, app-independent import target.
"""
